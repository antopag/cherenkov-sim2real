# results/

This directory is **gitignored**. Each experiment run writes into

    results/<experiment_name>/<YYYYMMDD-HHMMSS>_seed<N>/

with at minimum a `manifest.json`, `metrics.json`, and
`predictions.parquet`. See CLAUDE.md §5 and §8.
