# configs/adaptation/

One YAML per adaptation method: `srconly.yaml`, `tgtonly.yaml`,
`finetune.yaml`, `coral.yaml`, `mmd.yaml`, `dann.yaml`,
`selftrain.yaml`, `tent.yaml`, `bnadapt.yaml`. See PLAN.md §4.
