# configs/experiment/

Composed configs that fix the full (data, model, adaptation, seed) tuple
for a single experiment cell. File names follow the experiment-name
convention in CLAUDE.md §8, e.g.
`mc-uci__tab__dann__seed5.yaml`.
