"""MMD-based feature alignment (multi-kernel MMD penalty during training).

To be implemented. See PLAN.md §4.2. Distinct from `shift/mmd.py`, which
*measures* MMD as a diagnostic rather than using it as a training loss.
"""
